#include <stdio.h>

int main(){
    int a;
    //scanf("%d", &a);
    //printf("The value of a is %d", a);

    float b;
    b = 1.4;
    printf("the value of b is %f\n", b); // comments 
    
    char c; // comments 
    c = 'a'; // comments 
    printf("the value of c is %c", c);
    return 0;
} 